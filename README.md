# Inverted-Search
An Inverted Search Engine implemented in C that indexes words from multiple text files and allows efficient searching to find which files contain a given word and how many times it appears.
